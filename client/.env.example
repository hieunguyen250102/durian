# URL of the game server when it is deployed separately (e.g. on Render).
# Leave unset in development (Vite proxies /socket.io to localhost:3210) or when the server also serves the client.
VITE_SERVER_URL=https://durian-server.onrender.com
